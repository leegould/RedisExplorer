<UserControl x:Class="RedisExplorer.Controls.KeySetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <!--<ListBox x:Name="KeyValuesListBox">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <TextBox Text="{Binding}"></TextBox>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>-->

    <ItemsControl ItemsSource="{Binding Path=KeyValuesListBox}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <TextBox Text="{Binding Path=Item}" />
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
    <!--<DataGrid x:Name="KeyValuesListBox" Margin="5,5,5,5" AutoGenerateColumns="false" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <DataTemplate>
            <TextBox cal:Bind.ModelWithoutContext="{Binding}" Text="{Binding}"></TextBox>
        </DataTemplate>
    </DataGrid>-->
</UserControl>
