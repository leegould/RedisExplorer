<UserControl x:Class="RedisExplorer.Controls.KeyListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <DataGrid AutoGenerateColumns="False" CanUserAddRows="True" ItemsSource="{Binding KeyValuesListBox}" BorderBrush="Green" BorderThickness="1">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Value" Binding="{Binding Path=Item}" Width="Auto"></DataGridTextColumn>
        </DataGrid.Columns>
    </DataGrid>

</UserControl>
